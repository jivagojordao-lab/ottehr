services:
  postgres:
    image: postgres:16-alpine
    container_name: ottehr-postgres
    restart: unless-stopped
    network_mode: host
    environment:
      POSTGRES_DB: medplum
      POSTGRES_USER: medplum
      POSTGRES_PASSWORD: medplum_dev_password
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U medplum -d medplum -h 127.0.0.1"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: ottehr-redis
    restart: unless-stopped
    network_mode: host
    command: ["redis-server", "--protected-mode", "no"]
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-h", "127.0.0.1", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  medplum:
    image: medplum/medplum-server:latest
    container_name: ottehr-medplum
    restart: unless-stopped
    network_mode: host
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      MEDPLUM_BASE_URL: "http://localhost:8103/"
      MEDPLUM_PORT: 8103
      MEDPLUM_DATABASE_HOST: localhost
      MEDPLUM_DATABASE_PORT: 5432
      MEDPLUM_DATABASE_NAME: medplum
      MEDPLUM_DATABASE_USERNAME: medplum
      MEDPLUM_DATABASE_PASSWORD: medplum_dev_password
      MEDPLUM_REDIS_HOST: localhost
      MEDPLUM_REDIS_PORT: 6379
      MEDPLUM_REGISTER_ENABLED: "true"
    volumes:
      - ./medplum.config.json:/usr/src/medplum/medplum.config.json
      - ./medplum.config.json:/usr/src/medplum/packages/server/medplum.config.json
      - medplumdata:/var/lib/medplum/data

volumes:
  pgdata:
  redisdata:
  medplumdata:
